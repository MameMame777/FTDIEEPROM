# Windows Driver Matrix

| Channel | Use on Windows | Driver |
| --- | --- | --- |
| A | Vivado hw_server JTAG | FTDI standard D2XX driver |
| B | Unused | FTDI standard D2XX driver |
| C | Unused | FTDI standard D2XX driver |
| D | Unused | FTDI standard D2XX driver |

このプロジェクトでは Windows 上で EEPROM ツールを動かす前提にしていません。Linux 側で設定済みのデバイスを Windows に移して、Vivado 認識だけを確認します。